import numpy as np
import tensorflow.compat.v1 as tf

# 定义维张量

#input = tf.convert_to_tensor(np.random.random((1, 2, 3)).astype(np.float64))

# 张量变形,5维变成4维
#input = [tf.squeeze(t, axis=1) for t in tf.split(input, num_or_size_splits=1, axis=1)]
#input = tf.concat(input,axis=0)
